#ifndef FUNCS_H
#define FUNCS_H

#include <iostream>
#include <vector>

// TODO: Make the printVector function also work with vector<int> You can either
// overload the printVector function for std::vector<int> and have two versions
// of it, or you can changed printVector to be a template. The later is a much
// better solution since it would work for any vector


void printVector(std::vector<double> vec);

#endif
